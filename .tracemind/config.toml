# TraceMind project configuration

port = 7777
log_level = "info"

[llm]
provider = "anthropic"   # "anthropic" | "openai" | "ollama"
model = "claude-haiku-4-5"

# [verify]
# test_command = "cargo test"
# drift_warning_threshold = 0.3
