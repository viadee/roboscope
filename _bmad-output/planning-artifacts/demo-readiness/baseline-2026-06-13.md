# Regression baseline — 2026-06-13 (branch `chore/demo-readiness-bmad`, off `main`)

Pre-fix reference state (runs started before the Pass-3 execution fixes; pytest
imports at collection time, so this reflects `main`).

| Layer | Result |
|---|---|
| Frontend `vitest run` | ✅ GREEN (exit 0) |
| Backend `pytest` (`-m 'not integration'`) | ✅ **2054 passed, 43 deselected** (1179 warnings) in ~117 min |
| Backend integration (`-m integration`) | not run in baseline (needs Chromium; the 43 deselected) |
| E2E Playwright | not yet run for this branch (needs servers; scheduled) |

This is the green floor every subsequent pass must preserve. Post-fix
execution-suite regression tracked in the iteration log.
