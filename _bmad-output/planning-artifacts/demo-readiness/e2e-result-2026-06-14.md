# E2E pipeline result — 2026-06-14 (branch `chore/demo-readiness-bmad`)

Full local Playwright run: servers booted (backend :8000 + frontend :5173),
`CI=true npx playwright test` (screenshots/video skipped).

## Result: **296 passed, 2 failed, 6 skipped** (8.8 min)

This exercises features across areas A–K through the real UI — the
end-to-end verification backbone for demo-readiness.

## The 2 failures — NOT regressions from Passes 3–5
Backend log during the run: **POST /api/v1/runs returned 201 ×8, zero 500s,
zero tracebacks, no DetachedInstance** — the Pass-3 `start_run` commit change
(H3) did not break run start. Both failures are frontend/test-timing:

1. `execution-run.spec.ts:209` — "can start a run from explorer and see
   overlay": `getByText('Testlauf gestartet')` not visible within 10 s. The
   run started fine server-side (201); the success toast is timing-sensitive
   (appears then auto-dismisses). Classified via targeted retry run.
2. `stats.spec.ts:141` — "should show refresh button": 1.2 s quick assertion
   fail, pure frontend selector — independent of all backend changes.

## Classification
Targeted re-run of the two specs with `--retries=1` determines flaky vs.
real (see iteration log). If flaky → harden the test waits (real fix, not a
skip). If real & pre-existing on `main` → fix at source per DoD.


## Resolution (Pass 6) — both green on rerun (18 passed)
1. `stats.spec.ts:141` (consistent) — ROOT CAUSE: two buttons shared the
   accessible name "Aktualisieren" (toolbar refresh + stale-banner CTA, both
   `stats.refresh`). Fix: the stale-banner CTA now uses a distinct
   `stats.refreshNow` label ("Jetzt aktualisieren" / "Refresh now" /
   "Actualiser maintenant" / "Actualizar ahora", all 4 locales). Real a11y +
   demo-clarity fix, not a test workaround.
2. `execution-run.spec.ts:209` (flaky) — assertion moved off the transient
   "Testlauf gestartet" toast onto the persistent `.run-overlay-success`
   overlay (+ cold-run timeout). The toast auto-dismissed before polling.

Frontend after the i18n change: vitest 734 passed, prod build clean
(vue-i18n escape gate OK). Targeted rerun of both specs: 18 passed.
A final full-suite run confirms the whole pipeline green.
