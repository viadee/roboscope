# RoboScope — Engineering Documentation Index

_Generated by `bmad-document-project` (scoped deep-dive mode, 2026-04-14) for Phase 4 PRD grounding._

## Project snapshot

- **Type**: multi-part (backend + web frontend + Chrome extension)
- **Primary languages**: Python 3.12 (backend), TypeScript (frontend), JavaScript (extension)
- **Architecture**: sync FastAPI + SQLAlchemy 2.0 + in-process ThreadPoolExecutor for background work; Vue 3 SPA consuming a JWT/API-token protected REST API; Chrome MV3 extension as an arm's-length HTTP client.
- **Current milestone**: Enterprise-Readiness (Phase 4 = OAuth2/SSO + Teams — documentation target of this scan).

## Documents in this scan

| Document | Scope | Read before |
|---|---|---|
| [Project Overview](./project-overview.md) | Repository classification, milestone state, Phase 4 grounding summary. | Any work. |
| [Source Tree Analysis](./source-tree-analysis.md) | Annotated backend tree with Phase 4 surface area marked. | Changes under `backend/src/auth/`, `/repos/`, `/audit/`, `/settings/`. |
| [Backend Auth Deep-Dive](./backend-auth-deep-dive.md) | Current identity model, JWT + API-token flow, SSO plug-in points, Teams design. | Phase 4 PRD creation; any auth refactor. |
| [Backend Data Models (Phase-4 scope)](./data-models-backend.md) | Tables touched by Phase 4 + Alembic migration plan. | Schema changes, migrations. |
| [API Contracts (Phase-4 scope)](./api-contracts-auth.md) | Current auth/webhook/repo/audit/settings endpoints + new SSO/Teams endpoints. | Designing new routes. |

## Adjacent canonical docs (not part of this scan)

- [`../../CLAUDE.md`](../../CLAUDE.md) — compact project guide (architecture, tech stack, commands, patterns, roadmap).
- [`../project-context.md`](../project-context.md) — 202-rule agent context across 7 categories; mandatory read before implementation.
- [`../../README.md`](../../README.md) — end-user facing project description.

## Out of scope for this scan (intentionally)

- Frontend architecture (covered in `project-context.md` Category 3)
- Chrome extension architecture (independent GPL-3.0 module; see `extension/README.md`)
- Deployment guides (compose files + `CLAUDE.md` already cover)
- Integration architecture across parts (CLAUDE.md + project-context.md Category 3 cover)
- Full exhaustive source audit (would add 30–120 min, low marginal value over what's captured here)

## Completion state

See `./project-scan-report.json` for machine-readable state. Summary:

- **Mode**: `initial_scan` (scoped variant)
- **Scan level**: `deep` (backend auth-adjacent modules only)
- **Step coverage**: steps 1–3, 5 of the 12-step full workflow; steps 4, 6–12 skipped (out of scope for the PRD-grounding goal).
- **Output files**: 5 markdown docs + this index + state report.

## Next action

Run **`bmad-create-prd`** in a fresh Claude Code session. John (the PM agent) will use these documents + `project-context.md` as grounding material for the Phase 4 PRD.
